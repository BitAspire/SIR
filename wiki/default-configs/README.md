# Default Configs

Carpeta de configuraciones por defecto del plugin base.

## Nota
`plugin.yml` fue excluido intencionalmente.

## Archivos
- `bossbars.yml`
- `config.yml`
- `webhooks.yml`
- `commands/lang.yml`
- `commands/main/lang.yml`

## Fuente
`plugin/src/main/resources` (excepto `plugin.yml`).
